apiVersion: v1
kind: Service
metadata:
  name: {{ include "images-filters.fullname" . }}
  labels:
    {{- include "images-filters.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "images-filters.selectorLabels" . | nindent 4 }}
