apiVersion: v1
kind: Secret
metadata:
  name: images-filters-secret
  labels:
    app.kubernetes.io/name: images-filters
    app.kubernetes.io/component: secret
type: Opaque
data:
  # Base64 encoded values - 使用時請替換
  # echo -n 'your-redis-password' | base64
  REDIS_PASSWORD: ""
  # echo -n 'your-security-key' | base64
  SECURITY_KEY: ""
