apiVersion: v1
kind: Service
metadata:
  name: images-filters
  labels:
    app.kubernetes.io/name: images-filters
    app.kubernetes.io/component: server
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: images-filters
